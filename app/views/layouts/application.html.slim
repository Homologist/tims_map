doctype html
html
  head
    title TimsFeed
    = csrf_meta_tags
    = csp_meta_tag
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload'
    = javascript_include_tag 'application', 'data-turbolinks-track': 'reload'
    script src=("//maps.google.com/maps/api/js?key=AIzaSyDRDSIEs27gE6n9DGHdOQphUek2qG0Dktk")
    script src="//cdn.rawgit.com/mahnunchik/markerclustererplus/master/dist/markerclusterer.min.js"
    script src="//cdn.rawgit.com/printercu/google-maps-utility-library-v3-read-only/master/infobox/src/infobox_packed.js" type="text/javascript" ! only if you need custom infoboxes
  body

    = yield

    javascript:
      function loadMap() {
        var lat = 51.509865
        var lng = -0.118092
        var mapOptions = {
          center: new google.maps.LatLng(lat, lng),
          zoom: 12
        };
        handler = Gmaps.build('Google');
        handler.buildMap({ provider: mapOptions, internal: {id: 'map'}}, function(){
          markers = handler.addMarkers([
            {
              "lat": lat,
              "lng": lng
            }
          ]);
          handler.bounds.extendWith(markers);
        })
      }
      google.maps.event.addDomListener(window, "load", loadMap);
